(ns identika.protocols
  "⚠️ DEPRECATED — Protocols have been replaced by multimethod dispatch.

  Identika now uses Clojure multimethods dispatched on strategy keywords
  (:uuid, :ulid). This namespace is retained for backward compatibility only
  and will be removed in a future release.

  See identika.core for the current API.")
